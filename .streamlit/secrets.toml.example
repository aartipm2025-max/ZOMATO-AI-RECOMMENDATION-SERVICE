# Streamlit Cloud secrets — copy this file to .streamlit/secrets.toml
# and fill in your real values. NEVER commit secrets.toml to git.
#
# On Streamlit Cloud, go to:
#   App → Settings → Secrets
# and paste the contents of this file with your real API key.

GROQ_API_KEY = "gsk_your_real_key_here"
GROQ_MODEL   = "llama-3.3-70b-versatile"

# Optional: override the DB URL (leave blank to use the default SQLite file)
# ZOMATO_DB_URL = "sqlite:///./zomato_restaurants.db"
