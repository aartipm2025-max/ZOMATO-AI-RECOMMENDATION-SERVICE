"""Tests for Phase 3 (Groq orchestration and prompting).

These tests do NOT call the real Groq API; they only verify prompt building
and parsing. Integration with the real API is exercised manually once a
GROQ_API_KEY is configured.
"""

